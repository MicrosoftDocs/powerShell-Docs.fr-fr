---
ms.date: 08/24/2018
keywords: powershell,applet de commande
title: Apprentissage des noms de commande PowerShell
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 8d50ca03f98ed4ca8f9c09c83ae57afbf0d7888d
ms.sourcegitcommit: e7445ba8203da304286c591ff513900ad1c244a4
ms.translationtype: HT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/23/2019
ms.locfileid: "62086423"
---
# <a name="learning-powershell-command-names"></a><span data-ttu-id="a44a5-103">Apprentissage des noms de commande PowerShell</span><span class="sxs-lookup"><span data-stu-id="a44a5-103">Learning PowerShell command names</span></span>

<span data-ttu-id="a44a5-104">L’apprentissage des noms de commandes et des paramètres prend beaucoup de temps avec la plupart des interfaces de ligne de commande.</span><span class="sxs-lookup"><span data-stu-id="a44a5-104">Learning names of commands and parameters requires a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="a44a5-105">Le problème est qu’il y a peu de modèles.</span><span class="sxs-lookup"><span data-stu-id="a44a5-105">The issue is that there are few patterns.</span></span> <span data-ttu-id="a44a5-106">La mémorisation est le seul moyen d’assimiler les commandes et paramètres utilisés régulièrement.</span><span class="sxs-lookup"><span data-stu-id="a44a5-106">Memorization is the only way to learn the commands and parameters that you need to use on a regular basis.</span></span>

<span data-ttu-id="a44a5-107">Face à une nouvelle commande ou à un nouveau paramètre, on ne peut pas toujours utiliser ce qu’on connaît déjà.</span><span class="sxs-lookup"><span data-stu-id="a44a5-107">When you work with a new command or parameter, you can't always use what you already know.</span></span> <span data-ttu-id="a44a5-108">Il faut trouver son nom et le retenir.</span><span class="sxs-lookup"><span data-stu-id="a44a5-108">You have to find and learn a new name.</span></span> <span data-ttu-id="a44a5-109">En règle générale, les interfaces de ligne de commande commencent par un petit éventail d’outils et évoluent par ajouts successifs.</span><span class="sxs-lookup"><span data-stu-id="a44a5-109">Traditionally, command-line interfaces start with a small set of tools and grow with incremental additions.</span></span> <span data-ttu-id="a44a5-110">On comprend facilement pourquoi il n’existe aucune structure standard.</span><span class="sxs-lookup"><span data-stu-id="a44a5-110">It's easy to see why there's no standard structure.</span></span>
<span data-ttu-id="a44a5-111">En effet, chaque commande constitue un outil distinct.</span><span class="sxs-lookup"><span data-stu-id="a44a5-111">This seems logical for command names since each command is a separate tool.</span></span> <span data-ttu-id="a44a5-112">PowerShell présente une meilleure façon de gérer les noms de commandes.</span><span class="sxs-lookup"><span data-stu-id="a44a5-112">PowerShell has a better way to handle command names.</span></span>

## <a name="learning-command-names-in-traditional-shells"></a><span data-ttu-id="a44a5-113">Apprendre les noms de commandes dans des interpréteurs de commandes traditionnels</span><span class="sxs-lookup"><span data-stu-id="a44a5-113">Learning command names in traditional shells</span></span>

<span data-ttu-id="a44a5-114">La plupart des commandes sont conçues pour gérer des éléments du système d’exploitation ou des applications, telles que des services ou processus.</span><span class="sxs-lookup"><span data-stu-id="a44a5-114">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="a44a5-115">Les commandes portent divers noms qui peuvent ou non appartenir à une famille.</span><span class="sxs-lookup"><span data-stu-id="a44a5-115">The commands have names that may or may not fit into a family.</span></span> <span data-ttu-id="a44a5-116">Par exemple, sur les systèmes Windows, les commandes `net start` et `net stop` permettent de démarrer et d’arrêter un service.</span><span class="sxs-lookup"><span data-stu-id="a44a5-116">For example, on Windows systems, you can use the `net start` and `net stop` commands to start and stop a service.</span></span> <span data-ttu-id="a44a5-117">**sc.exe** est un autre outil de contrôle des services pour Windows.</span><span class="sxs-lookup"><span data-stu-id="a44a5-117">**Sc.exe** is another service control tool for Windows.</span></span> <span data-ttu-id="a44a5-118">Ce nom ne correspond pas au modèle de noms des commandes de service **net.exe**.</span><span class="sxs-lookup"><span data-stu-id="a44a5-118">That name does not fit into the naming pattern for the **net.exe** service commands.</span></span> <span data-ttu-id="a44a5-119">En ce qui concerne la gestion des processus, Windows offre la commande **tasklist.exe** pour les lister et la commande **taskkill.exe** pour les tuer.</span><span class="sxs-lookup"><span data-stu-id="a44a5-119">For process management, Windows has the **tasklist.exe** command to list processes and the **taskkill.exe** command to kill processes.</span></span>

<span data-ttu-id="a44a5-120">Par ailleurs, ces commandes ont des spécifications de paramètres irrégulières.</span><span class="sxs-lookup"><span data-stu-id="a44a5-120">Also, these commands have irregular parameter specifications.</span></span> <span data-ttu-id="a44a5-121">On ne peut pas utiliser la commande `net start` pour démarrer un service sur un ordinateur distant.</span><span class="sxs-lookup"><span data-stu-id="a44a5-121">You can't use the `net start` command to start a service on a remote computer.</span></span> <span data-ttu-id="a44a5-122">C’est la commande **sc.exe** qui permet de le faire.</span><span class="sxs-lookup"><span data-stu-id="a44a5-122">The **sc.exe** command can start a service on a remote computer.</span></span> <span data-ttu-id="a44a5-123">Mais, pour spécifier l’ordinateur distant, il faut faire précéder son nom de deux barres obliques inverses.</span><span class="sxs-lookup"><span data-stu-id="a44a5-123">But to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="a44a5-124">Pour démarrer le service spouleur sur un ordinateur distant nommé DC01, vous taperez `sc.exe \\DC01 start spooler`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-124">To start the spooler service on a remote computer named DC01, you type `sc.exe \\DC01 start spooler`.</span></span>
<span data-ttu-id="a44a5-125">Pour lister les tâches en cours d’exécution sur DC01, vous utiliserez le paramètre **/S** et le nom de l’ordinateur sans les barres obliques inverses.</span><span class="sxs-lookup"><span data-stu-id="a44a5-125">To list tasks running on DC01, you use the **/S** parameter and the computer name without backslashes.</span></span> <span data-ttu-id="a44a5-126">Par exemple, `tasklist /S DC01`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-126">For example, `tasklist /S DC01`.</span></span>

> [!NOTE]
> <span data-ttu-id="a44a5-127">Avant PowerShell v6, `sc` était un alias de la cmdlet `Set-Content`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-127">Prior to PowerShell v6, `sc` was an alias for the `Set-Content` cmdlet.</span></span> <span data-ttu-id="a44a5-128">Par conséquent, pour exécuter la commande **sc.exe** dans une version de PowerShell antérieure à v6, vous devez inclure le nom de fichier complet **sc.exe** , y compris l’extension de fichier **exe**.</span><span class="sxs-lookup"><span data-stu-id="a44a5-128">Therefore, to run the **sc.exe** command in a version of PowerShell prior to v6, you must include the full filename **sc.exe** including the file extension **exe**.</span></span>

<span data-ttu-id="a44a5-129">Les services et les processus sont des exemples d’éléments administrables d’un ordinateur qui ont des cycles de vie bien définis.</span><span class="sxs-lookup"><span data-stu-id="a44a5-129">Services and processes are examples of manageable elements on a computer that have well-defined life cycles.</span></span> <span data-ttu-id="a44a5-130">On peut les démarrer, les arrêter ou obtenir la liste de tous ceux qui sont en cours d’exécution.</span><span class="sxs-lookup"><span data-stu-id="a44a5-130">You may start or stop services and processes, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="a44a5-131">Malgré les différences techniques importantes qui les séparent, les actions effectuées sur les services et sur les processus sont conceptuellement identiques.</span><span class="sxs-lookup"><span data-stu-id="a44a5-131">Although there are important technical distinctions between them, the actions you perform on services and processes are conceptually the same.</span></span> <span data-ttu-id="a44a5-132">En outre, les choix de personnalisation d’une action par des paramètres peuvent également être similaires sur le plan conceptuel.</span><span class="sxs-lookup"><span data-stu-id="a44a5-132">Furthermore, the choices we make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="a44a5-133">PowerShell exploite ces ressemblances afin de réduire le nombre de noms distincts à connaître pour comprendre et utiliser les cmdlets.</span><span class="sxs-lookup"><span data-stu-id="a44a5-133">PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

## <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="a44a5-134">Les cmdlets utilisent des noms de type verbe-substantif pour alléger la mémorisation des commandes</span><span class="sxs-lookup"><span data-stu-id="a44a5-134">Cmdlets use verb-noun names to reduce command memorization</span></span>

<span data-ttu-id="a44a5-135">PowerShell utilise un système de noms de type « verbe-substantif ».</span><span class="sxs-lookup"><span data-stu-id="a44a5-135">PowerShell uses a "verb-noun" naming system.</span></span> <span data-ttu-id="a44a5-136">Chaque nom de cmdlet se compose d’un verbe standard suivi d’un trait d’union et d’un substantif spécifique.</span><span class="sxs-lookup"><span data-stu-id="a44a5-136">Each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="a44a5-137">Les verbes qu’utilise PowerShell ne sont pas toujours des verbes anglais, mais ils désignent des actions particulières dans PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a44a5-137">PowerShell verbs are not always English verbs, but they express specific actions in PowerShell.</span></span> <span data-ttu-id="a44a5-138">Les substantifs ressemblent aux noms communs que l’on trouve dans n’importe quelle langue.</span><span class="sxs-lookup"><span data-stu-id="a44a5-138">Nouns are very much like nouns in any language.</span></span> <span data-ttu-id="a44a5-139">Ils décrivent des types d’objets importants dans l’administration du système.</span><span class="sxs-lookup"><span data-stu-id="a44a5-139">They describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="a44a5-140">Il est facile de démontrer, en prenant quelques exemples, comment ces noms en deux parties contribuent à réduire l’effort d’apprentissage.</span><span class="sxs-lookup"><span data-stu-id="a44a5-140">It's easy to demonstrate how these two-part names reduce learning effort by looking at a few examples.</span></span>

<span data-ttu-id="a44a5-141">PowerShell comporte un jeu de verbes standard recommandé.</span><span class="sxs-lookup"><span data-stu-id="a44a5-141">PowerShell has a recommended set of standard verbs.</span></span> <span data-ttu-id="a44a5-142">Les noms, moins restreints, doivent toujours décrire ce sur quoi porte un verbe.</span><span class="sxs-lookup"><span data-stu-id="a44a5-142">Nouns are less restricted, but always describe what the verb acts upon.</span></span> <span data-ttu-id="a44a5-143">PowerShell offre des commandes comme `Get-Process`, `Stop-Process`, `Get-Service` et `Stop-Service`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-143">PowerShell has commands such as `Get-Process`, `Stop-Process`, `Get-Service`, and `Stop-Service`.</span></span>

<span data-ttu-id="a44a5-144">Dans cet exemple composé de deux noms et deux verbes, la cohérence ne simplifie pas tellement l’apprentissage.</span><span class="sxs-lookup"><span data-stu-id="a44a5-144">For this example of two nouns and verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="a44a5-145">Étendons cette liste à un ensemble standardisé de 10 verbes et 10 noms.</span><span class="sxs-lookup"><span data-stu-id="a44a5-145">Extend that list to a standardized set of 10 verbs and 10 nouns.</span></span> <span data-ttu-id="a44a5-146">Il n’y a que 20 mots à comprendre,</span><span class="sxs-lookup"><span data-stu-id="a44a5-146">Now you only have 20 words to understand.</span></span>
<span data-ttu-id="a44a5-147">mais ils peuvent être combinés pour former 100 noms de commandes distincts.</span><span class="sxs-lookup"><span data-stu-id="a44a5-147">But those words can be combined to form 100 distinct command names.</span></span>

<span data-ttu-id="a44a5-148">On comprend facilement ce que fait une commande PowerShell en lisant son nom.</span><span class="sxs-lookup"><span data-stu-id="a44a5-148">It's easy to understand what a PowerShell command does by reading its name.</span></span> <span data-ttu-id="a44a5-149">La commande permettant d’arrêter un ordinateur est `Stop-Computer`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-149">The command to shut down a computer is `Stop-Computer`.</span></span> <span data-ttu-id="a44a5-150">Celle qui liste tous les ordinateurs sur un réseau est `Get-Computer`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-150">The command to list all computers on a network is `Get-Computer`.</span></span> <span data-ttu-id="a44a5-151">Pour obtenir la date du système, la commande est `Get-Date`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-151">The command to get the system date is `Get-Date`.</span></span>

<span data-ttu-id="a44a5-152">Vous pouvez lister toutes les commandes comportant un verbe donné en ajoutant le paramètre **Verb** à `Get-Command`.</span><span class="sxs-lookup"><span data-stu-id="a44a5-152">You can list all commands that include a particular verb with the **Verb** parameter for `Get-Command`.</span></span> <span data-ttu-id="a44a5-153">Par exemple, pour afficher toutes les cmdlets qui utilisent le verbe `Get`, tapez :</span><span class="sxs-lookup"><span data-stu-id="a44a5-153">For example, to see all cmdlets that use the verb `Get`, type:</span></span>

```
PS> Get-Command -Verb Get

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="a44a5-154">Utilisez le paramètre **Noun** pour voir une famille de commandes qui affectent le même type d’objet.</span><span class="sxs-lookup"><span data-stu-id="a44a5-154">Use the **Noun** parameter to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="a44a5-155">Par exemple, exécutez la commande suivante afin de voir les commandes disponibles pour la gestion des services :</span><span class="sxs-lookup"><span data-stu-id="a44a5-155">For example, run following command to see the commands  available for managing services:</span></span>

```
PS> Get-Command -Noun Service

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

## <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="a44a5-156">Les cmdlets utilisent des paramètres standard</span><span class="sxs-lookup"><span data-stu-id="a44a5-156">Cmdlets use standard parameters</span></span>

<span data-ttu-id="a44a5-157">Comme nous l’avons mentionné, les commandes utilisées dans les interfaces de ligne de commande traditionnelles n’ont pas toujours des noms de paramètres cohérents.</span><span class="sxs-lookup"><span data-stu-id="a44a5-157">As noted earlier, commands used in traditional command-line interfaces don't always have consistent parameter names.</span></span> <span data-ttu-id="a44a5-158">Il s’agit souvent d’un caractère unique ou de mots abrégés, faciles à taper mais difficilement compréhensibles pour des néophytes.</span><span class="sxs-lookup"><span data-stu-id="a44a5-158">Parameters are often single-character or abbreviated words that are easy to type but aren't easily understood by new users.</span></span>

<span data-ttu-id="a44a5-159">Contrairement à la plupart des interfaces de ligne de commande classiques, PowerShell traite directement les paramètres et utilise cet accès direct aux paramètres ainsi qu’une aide aux développeurs pour normaliser les noms de paramètres.</span><span class="sxs-lookup"><span data-stu-id="a44a5-159">Unlike most other traditional command-line interfaces, PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="a44a5-160">Ces instructions encouragent à respecter le standard pour les différentes cmdlets, sans pour autant le garantir.</span><span class="sxs-lookup"><span data-stu-id="a44a5-160">This guidance encourages but does not guarantee that every cmdlet conforms to the standard.</span></span>

<span data-ttu-id="a44a5-161">PowerShell normalise également le séparateur de paramètre.</span><span class="sxs-lookup"><span data-stu-id="a44a5-161">PowerShell also standardizes the parameter separator.</span></span> <span data-ttu-id="a44a5-162">Les noms de paramètres sont toujours précédés d’un « - » avec une commande PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a44a5-162">Parameter names always have a '-' prepended to them with a PowerShell command.</span></span> <span data-ttu-id="a44a5-163">Prenons l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="a44a5-163">Consider the following example:</span></span>

```powershell
Get-Command -Name Clear-Host
```

<span data-ttu-id="a44a5-164">Le nom du paramètre est **Name**, mais il est noté `-Name` lorsqu’il est utilisé comme paramètre dans la ligne de commande.</span><span class="sxs-lookup"><span data-stu-id="a44a5-164">The parameter's name is **Name**, but it is typed as `-Name` when used on the command line as a parameter.</span></span>

<span data-ttu-id="a44a5-165">Voici quelques-unes des caractéristiques générales des noms et utilisations des paramètres standard.</span><span class="sxs-lookup"><span data-stu-id="a44a5-165">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

### <a name="the-help-parameter-"></a><span data-ttu-id="a44a5-166">Le paramètre Help (?)</span><span class="sxs-lookup"><span data-stu-id="a44a5-166">The Help parameter (?)</span></span>

<span data-ttu-id="a44a5-167">Lorsque vous spécifiez le paramètre `-?` dans une applet de commande, PowerShell affiche l’aide de cette applet.</span><span class="sxs-lookup"><span data-stu-id="a44a5-167">When you specify the `-?` parameter on any cmdlet, PowerShell displays help for the cmdlet.</span></span>
<span data-ttu-id="a44a5-168">La cmdlet n’est pas exécutée.</span><span class="sxs-lookup"><span data-stu-id="a44a5-168">The cmdlet is not executed.</span></span>

### <a name="common-parameters"></a><span data-ttu-id="a44a5-169">Paramètres communs</span><span class="sxs-lookup"><span data-stu-id="a44a5-169">Common parameters</span></span>

<span data-ttu-id="a44a5-170">PowerShell comporte plusieurs *paramètres communs*.</span><span class="sxs-lookup"><span data-stu-id="a44a5-170">PowerShell has several *common parameters*.</span></span> <span data-ttu-id="a44a5-171">Ils sont contrôlés par le moteur PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a44a5-171">These parameters are controlled by the PowerShell engine.</span></span> <span data-ttu-id="a44a5-172">Ils se comportent toujours de la même façon.</span><span class="sxs-lookup"><span data-stu-id="a44a5-172">Common parameters always behave the same way.</span></span> <span data-ttu-id="a44a5-173">Les paramètres communs sont **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable** et **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="a44a5-173">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

### <a name="recommended-parameter-names"></a><span data-ttu-id="a44a5-174">Noms de paramètres recommandés</span><span class="sxs-lookup"><span data-stu-id="a44a5-174">Recommended parameter names</span></span>

<span data-ttu-id="a44a5-175">Les principales cmdlet PowerShell utilisent des noms standard pour des paramètres similaires.</span><span class="sxs-lookup"><span data-stu-id="a44a5-175">The PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="a44a5-176">Bien que leur utilisation ne soit pas imposée, des instructions explicites encouragent la normalisation.</span><span class="sxs-lookup"><span data-stu-id="a44a5-176">The use of these standard names is not enforced, but there is explicit guidance to encourage standardization.</span></span>

<span data-ttu-id="a44a5-177">Par exemple, le nom recommandé pour un paramètre faisant référence à un ordinateur est **ComputerName**, plutôt que Server, Host, System, Node ou tout autre nom courant.</span><span class="sxs-lookup"><span data-stu-id="a44a5-177">For example, the recommended name for a parameter that refers to a computer is **ComputerName**, rather than Server, Host, System, Node, or some other common alternative.</span></span> <span data-ttu-id="a44a5-178">Voici d’autres noms de paramètres recommandés parmi les plus importants : **Force**, **Exclude**, **Include**, **PassThru**, **Path** et **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="a44a5-178">Other important recommended parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>
